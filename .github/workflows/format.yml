name: Format

on:
  pull_request:
    branches:
      - develop
      - sprint-1
      - sprint-2
      - sprint-3
      - sprint-4
      - sprint-5

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: |
          cd ui-kit
          npm install

      - name: Check code formatting with Prettier
        run: |
          cd ui-kit
          npm run ci:format

      - name: Check styles formatting with Stylelint
        run: |
          cd ui-kit
          npm run lint:styles
